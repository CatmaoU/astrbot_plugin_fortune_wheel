## v1.0.6 - 2026-08-10

### 修复
- **消息模板获取错误**：修复将 `message_templates` 改为列表后，`CoreMixin.get_message` 无法正确处理列表类型导致插件报错的问题。现在统一通过 `MessageManager` 获取消息，支持 `default` 返回值。
- **README 配置表重复**：删除重复的 `help_gift_penalty_multiplier` 行，默认值统一为 `1.0`。

### 优化
- **代码统一**：所有消息获取现在经过 `MessageManager`，便于维护。


## v1.0.5 - 2026-08-10

### 修复
- **替别人使用失败消息模板错误**：修复当普通成员替别人使用大礼包失败时，即使 `help_gift_penalty_multiplier` 为 1.0，仍错误走 `mute_success_template` 模板的问题。现在失败时强制使用 `mute_success_penalty` 模板，正确区分 `{user}`（实际被禁言者/发送者）和 `{target}`（原始目标/被替者）。
- **消息占位符区分**：替别人使用失败时，`{user}` 显示为发送者（受罚者），`{target}` 显示为被替的人，避免消息混淆。

### 优化
- **代码逻辑**：增加 `is_help_gift_failed` 标志，优先判断替别人失败状态，确保模板选择逻辑正确。


## v1.0.4 - 2026-08-09

### 修复
- **主程序导入错误**：移除 `main.py` 中已删除的 `CommonMixin` 导入，解决插件无法加载的问题
- **求饶系统硬编码消息**：将 `pardon.py` 中的硬编码消息替换为模板调用，支持自定义
- **插件卸载方法**：修复 `stop` 方法中调用不存在的 `super().stop()` 的问题
- **帮助文档路径**：修正 `README.md` 中错误的插件文件夹名称（`astrbot_plugin_gift_lottery` → `astrbot_plugin_fortune_wheel`）

### 优化
- **版本号统一**：`metadata.yaml` 和 `CHANGELOG.md` 同步为 v1.0.4
- **代码清理**：移除残留的 `CommonMixin` 引用，保持代码整洁


## v1.0.3 - 2026-08-09

### 新增
- `/大礼包历史` 指令：查看抽奖历史记录
- `/重载配置` 指令：热重载配置文件
- 每日抽奖次数限制（`daily_lottery_limit`，默认 -1 无限制）
- 指令冷却机制（`command_cooldown_seconds`，默认 15 秒）
- 群组黑白名单功能

### 优化
- 拆分 `vote_mixin.py` 为 `vote_mixin.py`、`petition_mixin.py`、`unmute_mixin.py`
- 删除冗余的 `common_mixin.py`
- 抽奖历史记录集成到抽奖流程
- 奖池配置缓存优化，减少 I/O 开销
- 帮助文档完善

### 修复
- 修复 `/大礼包历史` 永远无记录的问题
- 修复 `README.md` 中错误的插件路径
- 统一版本号与更新日志


## v1.0.2 - 2026-08-08

### 修复
- **权限管理**：修复权限无法正常获取，向下兼容，无论配置中 `global_admins` 是整数列表还是字符串列表，都能正常工作


## v1.0.1 - 2026-08-08

### 新增
- **全局管理员系统**：支持在配置文件中设置全局管理员列表，拥有最高权限（可执行所有管理指令），不受群管理员限制。
- **全新诅咒系统（每群独立）**：
  - 诅咒标记累积机制，每个用户可叠加多个标记
  - 概率触发：每个 `大礼包` 指令触发时，根据标记数计算触发概率（基础概率 + 每个标记增加概率）
  - 高级诅咒：当标记数超过阈值后，低权重物品获得额外权重加成
  - 全局诅咒：设置全局诅咒后，下一个使用 `/大礼包` 的用户会继承所有累计标记
  - 随机诅咒：随机选中群内用户并转移累计标记，发起者获得一半标记（四舍五入）
  - 诅咒排行榜：查看本群被诅咒最多的用户及总禁言时长
- **求饶系统优化**：用户可在多个群被禁言时，从列表中选择指定群进行求饶
- **每日使用限制**：`/诅咒` 指令支持每日使用次数限制（默认1次，可配置）
- **权限装饰器**：引入 `@require_permission` 装饰器，统一管理全局管理员与群管理员权限

### 修复
- **投票逻辑**：
  - 同意票达到阈值时立即解除禁言，不再等待投票结束
  - 投票结束后需同时满足：同意票 ≥ 最小票数 **且** 同意票 > 反对票，才能解禁
  - 修复投票期间目标被管理员解禁后，投票结束仍尝试重复解禁的问题
- **诅咒系统**：
  - 全局诅咒触发时，触发者继承 **全部** 累计标记而非仅 1 个
  - 随机诅咒中，目标获得全部累计标记，发起者获得一半（**四舍五入**，而非向下取整）
  - 高级诅咒在标记数达到 `curse_max_marks` 时立即触发（修复晚一次触发的问题）
  - 每日诅咒使用次数在跨天后自动重置
- **求饶系统**：
  - 修复求饶成功后重复重置次数的冗余逻辑
  - 求饶列表现在仅显示用户 **当前被禁言** 的群（从 `muted_cache.json` 读取）
- **配置文件**：将 `cache_config.txt` 自动迁移为 `cache_config.json`，确保兼容性
- **帮助系统**：将 `helps.json` 移至 `cache/` 目录，并自动从根目录迁移（若存在）

### 优化
- **代码重构**：将 `main.py` 拆分为多个 Mixin 文件（`_mixins/`），提高可维护性
- **插件显示名称**：统一为“大礼包轮盘”
- **帮助文档**：更新指令描述，添加新指令说明（`/随机诅咒`、`/诅咒排行榜`）
- **权限管理**：使用 `require_permission` 装饰器替代手动权限检查，代码更简洁
- **求饶提示**：当用户未被禁言时，仅提示“你当前没有被禁言的群喵～”，不再附带重置信息（内部重置逻辑保留）

### 其他
- 原插件 `astrbot_plugin_gift_lottery` 更名为 `astrbot_plugin_fortune_wheel`


## v1.0.0 - 2025-08-07

- 🎉 首次发布
- 支持基础轮盘抽奖、禁言
- 投票解禁系统
- 诅咒转移系统
- 求饶系统
- 缓存管理与自动刷新
- 完善配置与帮助文档

### 📦 依赖
- Pillow>=9.0.0
- astrbot>=4.26.0